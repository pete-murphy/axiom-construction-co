# Axiom Construction Co

### Local development instructions

Use `node >=14.4`

Install dependencies

```shell
npm install
```

Run `gatsby` development server

```bash
npm run develop
```

Deploy by committing changes and pushing to `master` branch
